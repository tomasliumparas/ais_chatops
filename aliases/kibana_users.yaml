---
name: chatops.kibana_users
action_ref: ais_chatops.kibana_users
description: Get a list of Kibana users
formats:
  - display: "kibana users"
    representation:
      - "kibana users"
result:
  format: |
    Kibana users ```{{ execution.result.stdout|regex_findall('(?=\"msg\": ")(.*)(?=\"') }}```
